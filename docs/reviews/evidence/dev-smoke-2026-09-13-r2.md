# dev 端真实冒烟（2026-09-13）——R2 四层节拍与桌面设置

数据隔离：`bun run dev:tauri` 使用 debug-dev 配置和 `~/.screenpipe-dev`，未触碰生产目录 `~/.screenpipe`，未调用真实产品模型。代码基线：`02c04d882`。

本次启动两次：第一次约 06:26–06:34，第二次约 06:40–06:41（日志时间为 UTC；桌面显示为 UTC+8）。

## 1. 通过项

| 项 | 证据 |
| --- | --- |
| 原生桌面启动 | 两次 `bun run dev:tauri` 均完成 native warm-up，应用进程启动，dev API 监听 `127.0.0.1:3130` |
| 健康状态 | `/health` 返回 `status=healthy`、`frame_status=ok`；三块显示器均被识别；音频为配置的 `meetings-only`，状态为 `waiting_for_meeting` |
| 13 个 R2 设置项 | 活动记录设置页 AX/UI 树出现“知识管线节拍”，包含 13 个数字字段及单位/范围；四层说明明确显示为独立调度 |
| legacy 标识 | UI 显示 `间隔 legacy`，并提示“仅影响旧版叙事生成（已停用自动生成）” |
| 设置持久化 | 桌面 UI 将 `① 间隔重建节拍` 改为 `6` 并离开字段后，隔离设置文件中 `knowledgeReconcileMinutes=6`；返回活动记录页可再次进入该设置 |
| 失败任务展示 | 设置页显示“失败的知识任务”、任务类型/ID、`模型返回空结果` 和 `窗口内没有可引用的证据行…` 等 `last_error_message`，每行有“手动重试”，顶部有“刷新失败任务” |
| ① 活动间隔重建 | 日志出现 `knowledge: ① activity ledger rebuilt intervals=3 window_hours=2` |
| ②/③ 发现投递 | 日志出现 `knowledge: enqueued 1 summarize jobs` 和 `knowledge: enqueued 20 extraction jobs`，说明独立 tick 已接通 |
| legacy 后端调度 | 日志出现 `legacy automatic narrative generation retired`；本次日志未出现 `running scheduled generation` |

## 2. 限定观察与残余风险

1. **④ 未在本次短冒烟内触发**：默认 `knowledgeDistillHours=24`，本次运行不足以证明 ④ 的实际定时触发；三机制与 ④ 全量投递已由 engine 定向测试覆盖。需要后续长时/虚拟时钟观察。
2. **隔离 dev 库存在历史积压**：应用启动后继续消费旧任务，日志出现 `model_error` / `no_evidence`。停止应用时隔离库统计为：`failed extract=868`、`failed summarize=377`、`failed compile=5`、`succeeded extract=210`、`succeeded summarize=103`、`succeeded compile=114`。这属于已有 dev 数据和模型配置的运行状态，不作为 R2 代码回归结论；生产库未被修改。
3. **未将手动重试计为行为通过**：按钮和错误内容已确认可见；本次点击后因 worker 同时消费积压，短时间内未获得稳定的“从失败列表移除”证据，因此只记录 UI 可用性，不伪称重试闭环已通过。
4. 日志另有 `frame_linker: stale entries expired without pairing` 警告；不属于本次 R2 改动范围，建议在长时观察中单独追踪。

## 3. 结论

R2 桌面设置面、隔离运行、①/②/③ tick 接线和 legacy 下线均通过本次真实冒烟。R2 的 ④ 真实周期触发、失败任务手动重试成功闭环以及积压清理仍需单独观察；在积压未处理前，不建议直接进行长时间真实模型运行。

相关代码验收：agent-brain acceptance 6/6，`task_status=completed`；原生构建、typecheck、engine/DB 定向测试均通过。
